
# ── Aurora v3.4 Network Tuning (RAM-optimized) ──────────────────
# BBR congestion control (2-3x faster than Cubic on lossy links)
net.core.default_qdisc=fq
net.ipv4.tcp_congestion_control=bbr

# v3.4: TCP buffer sizes reduced (was 64MB max, now 16MB)
# Previous values caused high RAM on small Railway containers
# during speed tests (each connection could allocate up to 64MB).
# 16MB is still large enough for high-BDP links but caps total usage.
net.ipv4.tcp_rmem=4096 87380 16777216
net.ipv4.tcp_wmem=4096 65536 16777216
net.core.rmem_max=16777216
net.core.wmem_max=16777216
net.core.rmem_default=131072
net.core.wmem_default=131072
net.core.netdev_max_backlog=2500

# Disable slow-start-after-idle (keep window open for keepalive connections)
net.ipv4.tcp_slow_start_after_idle=0

# Enable MTU probing (fixes black-hole MTU issues)
net.ipv4.tcp_mtu_probing=1

# TCP Fast Open (server-side, reduces 1 RTT on reconnect)
net.ipv4.tcp_fastopen=3

# Keepalive tuning (faster detection of dead connections)
net.ipv4.tcp_keepalive_time=600
net.ipv4.tcp_keepalive_intvl=15
net.ipv4.tcp_keepalive_probes=5

# v3.4: Reduced backlog (was 8192, now 2048) — sufficient for small servers
net.core.somaxconn=2048
net.ipv4.tcp_max_syn_backlog=2048

# Disable IPv6 (avoid AAAA delays when IPv6 is broken)
net.ipv6.conf.all.disable_ipv6=1
net.ipv6.conf.default.disable_ipv6=1
# ── End Network Tuning ──────────────────────────────────
